# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=dev_user
DB_PASSWORD=
DB_DATABASE=multiapp-release

# GitHub (from ~/.cmulti/.claude/profile.sh)
GH_MULTI_TOKEN=
GH_ORG=multi-develop

# Optional: override default path to repository-list.txt
# REPO_LIST_PATH=/path/to/repository-list.txt
